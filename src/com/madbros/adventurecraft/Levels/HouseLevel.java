package com.madbros.adventurecraft.Levels;

import com.madbros.adventurecraft.*;

public class HouseLevel extends Level {

	
	
	@Override
	public void render() {
//		for(int i = activeBlockPosX; i < activeBlockPosX + Game.renderWidth; i++) {
//			for(int j = activeBlockPosY; j < activeBlockPosY + Game.renderHeight; j++) {
//				if(i < activeBlocks.length && j < activeBlocks[0].length && i >= 0 && j >= 0) {
//					activeBlocks[i][j].render(TILE_SIZE * (i-activeBlockPosX) - offsetX - TILE_SIZE, 
//											  TILE_SIZE * (j-activeBlockPosY) - offsetY - TILE_SIZE);
//				}
//			}
//		}
	}
}
